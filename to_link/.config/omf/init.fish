# [ -f ~/.private_vars.sh ]; and source ~/.private_vars.sh
# [ -f ~/.bash_env ]; and source ~/.bash_env
# [ -f ~/.bash_paths ]; and source ~/.bash_paths
[ -f ~/.bash_aliases ]; and source ~/.bash_aliases
# [ -f ~/.bash_functions ]; and source ~/.bash_functions
# [ -f ~/.bash_completions ]; and source ~/.bash_completions
# [ -f ~/.promptline.theme.bash ]; and source ~/.promptline.theme.bash
# [[ -s "$HOME/.rvm/scripts/rvm" ]]; and source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
